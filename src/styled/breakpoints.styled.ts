const smallDesktop = 1024
const tablet = 768
const mobile = 524
const smallMobile = 380

export const breakpoints = {
  smallDesktop: `${smallDesktop}px`,
  tablet: `${tablet}px`,
  mobile: `${mobile}px`,
  smallMobile: `${smallMobile}px`
}
